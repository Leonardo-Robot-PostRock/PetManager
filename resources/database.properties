# PetManager — Configuración de base de datos
# Las credenciales sensibles van en variables de entorno:
#   PETMANAGER_DB_URL      → jdbc:mysql://localhost:3306/petmanager?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#   PETMANAGER_DB_USER     → tu usuario MySQL
#   PETMANAGER_DB_PASSWORD → tu contraseña MySQL
#
# Si no definís las variables de entorno, la app usa estos valores por defecto.

db.url=jdbc:mysql://localhost:3306/petmanager?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=fucks0101
db.driver=com.mysql.cj.jdbc.Driver